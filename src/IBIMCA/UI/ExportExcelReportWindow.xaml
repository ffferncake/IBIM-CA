<Window x:Class="IBIMCA.UI.ExportExcelReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="평가 내보내기"
        Height="290" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top header -->
        <Border Grid.Row="0" Background="#F3F3F3" Padding="8" BorderBrush="#D0D0D0" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="평가 내보내기" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Width="24" Height="24" Margin="6,0,0,0" Content="X" ToolTip="닫기" Click="OnClose"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Group 1 -->
        <GroupBox Grid.Row="1" Header="평가항목 선택 내보내기" Margin="0,10,0,0">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="34"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Height="26"
                         VerticalContentAlignment="Center"
                         Text="{Binding SelectedItemsPath, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="파일 경로"/>

                <Button Grid.Column="1"
                        Height="26"
                        Margin="6,0,0,0"
                        Content="✔"
                        ToolTip="경로 선택"
                        Command="{Binding BrowseSelectedItemsCommand}"/>
            </Grid>
        </GroupBox>

        <!-- Group 2 -->
        <GroupBox Grid.Row="2" Header="자체평가 내보내기" Margin="0,10,0,0">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="34"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Height="26"
                         VerticalContentAlignment="Center"
                         Text="{Binding SelfEvaluationPath, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="파일 경로"/>

                <Button Grid.Column="1"
                        Height="26"
                        Margin="6,0,0,0"
                        Content="✔"
                        ToolTip="경로 선택"
                        Command="{Binding BrowseSelfEvaluationCommand}"/>
            </Grid>
        </GroupBox>

        <!-- Bottom -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Width="36" Height="30"
                    Content="◀"
                    ToolTip="뒤로"/>

            <Button Grid.Column="2"
                    Width="90" Height="30"
                    Content="완료"
                    Command="{Binding FinishCommand}"/>
        </Grid>
    </Grid>
</Window>